# php-interview
Application containing 3 recommendation algorithms:  Returns 3 random titles. Returns all movies starting with the letter “W”, but only if their title has an even number of characters. Returns all titles that consist of more than one word. The application should include unit tests that verify the correctness of the solution.
